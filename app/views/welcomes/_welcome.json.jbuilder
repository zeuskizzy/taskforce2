json.extract! welcome, :id, :title, :logoimg, :desc, :whatsapp, :ig, :ln, :section, :link, :footer, :phone, :address, :twitter, :email, :youtube, :telegram, :created_at, :updated_at
json.url welcome_url(welcome, format: :json)
